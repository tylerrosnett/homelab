apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hello
  namespace: hello-public
spec:
  parentRefs:
    - name: cf-tunnel
      namespace: kube-system
  hostnames:
    - hello.tylerrosnett.com
  rules:
    - backendRefs:
        - name: hello
          port: 80
