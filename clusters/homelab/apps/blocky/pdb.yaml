apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: blocky
  namespace: blocky
spec:
  # LAN-wide DNS: every client on Trusted queries Blocky directly, so a drain must never take both pods.
  minAvailable: 1
  selector:
    matchLabels:
      app: blocky
