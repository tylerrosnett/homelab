apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: game-2048
  namespace: game-2048
spec:
  interval: 30m
  chart:
    spec:
      chart: game-2048
      version: 0.1.2
      sourceRef:
        kind: HelmRepository
        name: devpro
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
